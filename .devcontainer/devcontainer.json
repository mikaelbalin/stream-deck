{
  "name": "Tauri Dev Container",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "remoteUser": "vscode",
  "containerEnv": {
    "SHELL": "/usr/bin/fish"
  },
  "customizations": {
    "zed": {
      "extensions": ["rust", "typescript"]
    },
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "tamasfe.even-better-toml",
        "tauri-apps.tauri-vscode"
      ]
    }
  },
  "postCreateCommand": "rustc --version && cargo --version && node --version && npm --version && fish --version"
}
